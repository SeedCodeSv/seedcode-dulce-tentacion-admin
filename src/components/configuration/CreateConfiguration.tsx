import { ChangeEvent, useEffect, useRef, useState } from "react";
import { toast } from "sonner";
import {
    Input,
    Select,
    SelectItem,
    Image as NextImage,
    Button,
    useDisclosure,
  } from "@nextui-org/react";

function CreateConfiguration() {
  return (
    <div>CreateConfiguration</div>
  )
}

export default CreateConfiguration