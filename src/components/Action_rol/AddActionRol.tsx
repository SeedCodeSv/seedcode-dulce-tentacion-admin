import { useState } from "react";

const AddActionRol = () => {
    const [showActions, setShowActions] = useState(false);
    const [nombres, setNombres] = useState<{ nombre: string }[]>([{ nombre: "Mostrar" }]);

  return (
    <div>
      
    </div>
  )
}

export default AddActionRol
