import { db } from "../db"
import { AddressCredito } from "../entities/address_credito_fiscal"

export const save_address_credito = async (address: AddressCredito) => {
    const id = await db.direccion
}